# Task state

Last updated: 2026-08-29

Current branch: `feat/sahayi-on-device-intelligence`, created and initially pushed from exact clean checkpoint `89be9a83a6f4d758207e4457771b66063fca4fc5` after fetch, upstream `0/0`, and ancestry verification. `main` is untouched. The compact multilingual on-device intent phase is implemented and verified, and is ready for its authorized final commit and normal branch push.

Sahayi now bundles a real trained dependency-free character 2–5-gram Multinomial Naive Bayes classifier. Python-standard-library training is deterministic and offline; browser inference is synchronous TypeScript with no ML runtime, WebGPU/WASM, browser LLM, model download, generation, or network request. Its fixed allowlisted classes are Aadhaar address update, Kerala old-age pension, and unsupported/other. A 500-code-point input cap, 3,308-entry vocabulary, capped feature counts, 0.85 validation-tuned confidence, and 0.40 top-two margin bound work. Invalid artifacts silently fall back to the deterministic pack-phrase matcher.

The reviewed versioned dataset contains 81 wholly synthetic Sahayi-owned examples, balanced at 27 per locale and label with 45 train, 18 validation, and 18 final-test rows. It includes English, native Hindi/Malayalam, transliteration, misspelling, mixed-script, unsupported, ambiguous, adversarial, prompt-like, emoji/control, repeated-character, short, conversational, and bounded-long cases. It contains no scraped citizen query, identifier-shaped value, or real PII. Hindi/Malayalam rows are explicitly marked machine-assisted and native-review-required. Validation rejects malformed rows/Unicode, invalid labels, missing provenance, duplicates, normalized cross-split leakage, imbalance, excessive inputs, and unbounded vocabulary.

Model SHA-256 is `fd8966853576dc1233a82908f93ce80a56d87537d0f0a82ef94d25a97adf54b4`; dataset SHA-256 is `14e56d1561584e6bfec420957d393740a9f3f6de53cbf6b965ad4a0084c58e62`. Canonical regeneration is byte-identical and build/test checks bind the artifact, sidecar, dataset, frontend expected digests, schema/model/normalization versions, labels/service allowlist, thresholds, finite weights, normalization vectors, and size.

On the 18-example held-out synthetic set, accuracy is 0.833333; macro precision/recall/F1 are 0.952381/0.833333/0.863248; abstention is 0.111111; accepted-prediction accuracy is 0.937500; exact deterministic-matcher/ML agreement when deterministic matching is confident is 1.0; unsupported false-positive rate is 0.166667. The confusion rows (Aadhaar, pension, unsupported) over columns (Aadhaar, pension, unsupported, abstain) are `6/0/0/0`, `0/6/0/0`, and `1/0/3/2`. These are synthetic-set diagnostics, not real-world accuracy claims.

The finder blocks obvious PII/identifiers before either matcher, then runs both. Confident agreement proposes the shared service; ML-only or deterministic-only confidence proposes with explicit confirmation; disagreement/ambiguity requires a choice and then confirmation; unsupported never claims a service; dual abstention preserves no-match. Only active-catalogue allowlisted IDs can be proposed. No model output affects eligibility, readiness, facts, fees, sources, or Procedure Packs.

Raw query and inference results remain in React memory. They are cleared on successful confirmation, language switch, Start Over, End session, and inactivity expiry. No query/result logging, telemetry, cookie, local/session storage, IndexedDB, Cache API, service worker, or model network request was introduced. Localized live-region text discloses on-device matching, no online transmission for the finder action, and required confirmation. The optional consent-gated cloud agent remains separate.

Verification: 163 backend tests and 59 frontend tests pass; lint, TypeScript, production build, 17 offline agent-eval tests, artifact drift/integrity checks, active-pack validation, schema drift, same-origin runtime, and diff checks pass. The artifact is 167,430 bytes uncompressed and 13,530 bytes gzip. Main JS is 453,578 bytes/96,271 bytes gzip; exact baseline delta is 172,396 bytes uncompressed and 15,737 bytes gzip. Environment-specific Chrome click-to-result observations were 4.9 ms English, 0.9 ms Hindi, and 0.6 ms Malayalam, with no timing assertion.

The exact no-network production container image `0c9caa5c4b4f3c61a9a32ee4f0a4273ec29885b567e3a6988fc81e7bac4b7af4` runs as UID/GID 10001 and passes same-origin health/root/hashed-asset checks with `Cache-Control: no-store` and an exact asset hash match. Eighteen graphical checks pass across English, Hindi, and Malayalam at 360, 390, 768, and 1280 pixels, including intent proposal/confirmation, zero intent resource requests, overflow, visible focus, live-region output, demo disclosure/status, trust information, and session clearing.

Dependency audits report no known npm/Python vulnerabilities (the local unpublished `sahayi-api` package is skipped by pip-audit). Secret, PII/identifier, persistence, logging/telemetry, network, generated-number, and dependency-drift scans are clean. No Procedure Pack file changed: active canonical digests remain UIDAI `595a62902a6145c82f02b9fbe361e7d8db5e34dea8d48e7e030aac7e43222bad` and Kerala `6eac396b47a82c2978b812cd4915dcf5dbc4ccac14ea8e4d93e2e57f28a53a76`.

Still external/manual: native-speaker review of every Hindi/Malayalam dataset phrase and localized UI message; broader representative, independently collected privacy-reviewed data; real-world calibration, fairness/error analysis, and accessibility review with users. The small synthetic test set—especially its one ambiguous supported-service false positive—must not be treated as production accuracy evidence. Deployment, live official-source retrieval, billable OpenAI evaluation, real form filling/submission/status, voice, and main-branch promotion remain unperformed.
